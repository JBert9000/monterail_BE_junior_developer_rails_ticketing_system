<h2>Current Cart</h2>

<table>
  <thead>
    <tr>
      <td>Event</td>
      <td>Price</td>
      <td>Quantity</td>
      <td></td>
    </tr>
  </thead>
  <tbody>

    <!-- < % @event.each do |event| %>
      < % event.name %>
    < % end %> -->
    <% @tickets.each do |ticket| %>
      <tr>

        <td><%= ticket.id %></td>

        <td>

          <%= form_for ticket, method: :delete do |f| %>
            <%= f.submit "Remove" %>
          <% end %>
        </td>
      </tr>

    <% end %>
  </tbody>
</table>
